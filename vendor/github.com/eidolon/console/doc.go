// Package console contains the core code that ties everything together. This includes the
// Application type that actually runs the commands and reacts to input, and also functions for
// parsing and mapping input that use other sub-packages.
package console
