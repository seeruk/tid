syntax = "proto3";

option go_package = "proto";

message Status {
    enum State {
        STARTED = 0;
        STOPPED = 1;
        PAUSED = 2;
    }

    State state = 1;
    string time_sheet_entry = 2;
}
