#!/bin/sh

# Check if ~/.tid exists, create it if not
# Check latest version
# Backup existing data (how do we know where the DB is? parse config for path?)
# Move old version (move ~/.tid/bin to ~/.tid/bin-{$VERSION})
# Download new version to ~/.tid/bin
# Rename it
# Make it executable (if not on Windows..?)
# Done?
